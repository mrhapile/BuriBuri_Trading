# Alpaca API Configuration (Paper Trading Only)
# Copy this file to .env and fill in your credentials
# NEVER commit .env to version control

# Frontend Configuration
# API URL must be set for the frontend to connect to the backend
VITE_API_URL=http://127.0.0.1:10000

# Backend Security Configuration
# ENV: 'development' or 'production'
ENV=development
# ALLOWED_ORIGINS: Comma-separated list of allowed frontend domains
# Required in production. In development, defaults to allowing all if empty.
ALLOWED_ORIGINS=http://localhost:5173,https://your-frontend.onrender.com

ALPACA_API_KEY= your_paper_api_key_here
ALPACA_SECRET_KEY= your_paper_secret_key_here
ALPACA_BASE_URL= https://paper-api.alpaca.markets