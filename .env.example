# =============================================================================
# BuriBuri Trading - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your credentials:
#   cp .env.example .env
#
# NEVER commit .env to version control!
# All variables are OPTIONAL - the system works without any credentials.
# =============================================================================


# =============================================================================
# ALPACA API (Optional - for live market data during market hours)
# =============================================================================
# Get free paper trading keys at: https://alpaca.markets
# These enable live portfolio data when the market is OPEN.
# Without them, the system uses cached historical data (works fine for demos).

ALPACA_API_KEY=
ALPACA_SECRET_KEY=
ALPACA_BASE_URL=https://paper-api.alpaca.markets


# =============================================================================
# POLYGON API (Optional - for sector candle data)
# =============================================================================
# Get a free API key at: https://polygon.io
# Used as a fallback for market data when Alpaca data is restricted.
# Without this, the system uses alternative data sources or cached data.

POLYGON_API_KEY=


# =============================================================================
# SERVER CONFIGURATION (Optional - defaults work for local development)
# =============================================================================
# These are typically only changed for production deployments.
# Render and other platforms set PORT automatically.

# PORT=10000
# HOST=0.0.0.0


# =============================================================================
# HISTORICAL VALIDATION MODE (Optional - for advanced testing)
# =============================================================================
# Set to "true" to run historical validation instead of normal operation.
# Used for backtesting decision logic over extended periods.

# HISTORICAL_VALIDATION=false

# Symbol and time range for validation mode
# VAL_SYMBOL=SPY
# VAL_RANGE=6M